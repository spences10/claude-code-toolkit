{
  "description": "Toolkit-skills plugin hooks. UserPromptSubmit forces skill evaluation. Stop hook suggests /reflect for sessions with learnings.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/skill-activation-forced-eval.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "model": "sonnet",
            "prompt": "Evaluate if this session had corrections, discoveries, or validated patterns worth remembering. If significant learnings found that should be persisted, the condition is NOT met - suggest running /reflect. Be selective - only block for sessions with clear teachable moments. Most sessions should pass."
          }
        ]
      }
    ]
  }
}
